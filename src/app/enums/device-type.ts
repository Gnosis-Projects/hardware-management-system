export enum DeviceType {
  COMPUTER = 'computer',
  PHONE = 'phone',
  WORKSTATION = 'workstation',
  PRINTER = 'printer',
  NETWORK_EQUIPMENT = 'network_equipment',
  SERVER = 'server'
}

export enum RemoteDesktopAppType {
  TeamViewer = 1,
  AnyDesk = 2,
}

export enum PrinterType {
  BASIC = 1,
  NETWORK = 2,
}

export enum NetEquipmentType{
  ROUTER = 1,
  SWITCH = 2,
}

